import { StyleSheet } from 'react-native';
import mainColors from '../../themes/colors';

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: mainColors.main_yellow,
  },
});

export default styles;
